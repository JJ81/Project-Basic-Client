<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        {{current_path}}
        {{#if (isEquals current_path 'Agent Balance History') }}: <strong class="text-warning">{{agentName}}</strong> {{/if}}
        {{#if (isEquals current_path 'Player Balance History') }}: <strong class="text-warning">{{player}}</strong> {{/if}}
    </h1>
    <ol class="breadcrumb">
        <li><a href="/"><i class="fa fa-home"></i> Home</a></li>
        <li class="active">{{current_path}}</li>
    </ol>
</section>
<!-- // page header -->